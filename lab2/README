This lab was implemented on Jupyter with Python 3. Both are required to run this, as well as the packages numpy, pandas, matplotlib, and scikit-learn.
Code also assumes that folder final_lab2_data is in the local directory, as well as car_traces.txt to distinguish which data are from which car.
