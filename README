For most formatted writeup, run classify.ipynb. If Jupyter is not available, classify.py gives the same results and has the same markup. Notebook and script both require folders activitydataset and activity-test-dataset in the local directory.
