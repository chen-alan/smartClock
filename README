For most formatted writeup, run classify.ipynb. If Jupyter is not available, classify.py gives the same results and has the same markup. Both require folders activitydataset and activity-test-dataset in the directory they are run in.
